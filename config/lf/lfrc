map i !hterm-show-file.sh "$f"

set ifs "\n"

cmd trash %trash-put $fx
map <delete> trash

cmd chrome %google-chrome-stable $fx
map <enter> chrome

# config/lf/lfrc
# Copyright Keith Maxwell
# SPDX-License-Identifier: CC0-1.0
